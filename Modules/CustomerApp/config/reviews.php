<?php

/**
 * تنظیمات نظرسنجی پایان سفارش (Customer App).
 *
 * فقط سفارش‌های completed که در بازه‌ی اخیر تکمیل شده‌اند، نظرسنجیِ اجباری
 * دارند؛ تا کاربری با سابقه‌ی طولانی موقع باز کردن اپ با صفِ بلندِ نظرسنجی
 * روبه‌رو نشود. این بازه هم روی لیست pending-reviews و هم روی enforcement
 * (بلاک ثبت سفارش جدید) اعمال می‌شود تا UX یکدست بماند.
 */
return [
    // بازه‌ی زمانی (روز) برای نظرسنجیِ اجباری. 0 = بدون محدودیت (کل تاریخچه).
    'pending_window_days' => (int) env('CUSTOMER_PENDING_REVIEW_WINDOW_DAYS', 30),
];
