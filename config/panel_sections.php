<?php

/*
|--------------------------------------------------------------------------
| بخش‌های بازنشسته/غیرفعالِ پنل
|--------------------------------------------------------------------------
|
| این بخش‌ها هم از نوارِ کناری حذف شده‌اند و هم دسترسیِ مستقیم به مسیرهایشان
| مسدود است (middleware: BlockRetiredSections → 404). برای بازگرداندنِ یک
| بخش، الگوی نامِ مسیرِ آن را از این لیست بردار (و در صورت نیاز آیتمِ نوارِ
| کناری را در admin.blade.php برگردان).
|
| نکته: «حقوق و دستمزد» (salary) به‌صورتِ موقت غیرفعال است تا زمانِ
| بهره‌برداریِ مجدد؛ برای فعال‌سازیِ دوباره فقط خطِ salary.* را حذف کن و
| در layout بلاکِ @if(false) را به @if(true) برگردان.
|
*/

return [

    'disabled_route_patterns' => [
        // کارتابل پرسنلی (حضور و غیاب + مرخصی)
        'attendance.*',
        'leave.*',

        // مدیریت OKR
        'okr.*',

        // مدیریت انبار
        'warehouse.*',

        // حقوق و دستمزد — غیرفعالِ موقت تا زمانِ بهره‌برداریِ مجدد
        'salary.*',
    ],

];
